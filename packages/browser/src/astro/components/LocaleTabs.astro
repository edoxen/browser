---
interface Props {
  spellings: readonly string[]
  active: string
  /** Unique key matching the [data-tab-target] blocks this tab row switches. */
  field: string
}
const { spellings, active, field } = Astro.props
---

{/* Wrapped in the <section-tabs> island: clicking a tab toggles the
    [data-tab-target="{field}"] blocks' hidden state client-side. */}
{spellings.length > 1 && (
  <section-tabs data-target={field} class="edoxen-locale-tabs" aria-label={`Available languages for ${field}`} data-field={field}>
    {spellings.map((sp) => (
      <button type="button" class="edoxen-locale-tabs__tab" data-spelling={sp} aria-pressed={sp === active}>
        {sp}
      </button>
    ))}
  </section-tabs>
)}

<style>
.edoxen-locale-tabs {
  display: inline-flex;
  gap: 0.25rem;
  margin: 0.5rem 0 1rem;
  padding: 0.25rem;
  border: 1px solid var(--edoxen-color-border);
  border-radius: 9999px;
  background: color-mix(in srgb, var(--edoxen-color-text) 4%, transparent);
  width: fit-content;
}
.edoxen-locale-tabs__tab {
  border: 0;
  background: transparent;
  color: var(--edoxen-color-muted);
  padding: 0.25rem 0.75rem;
  border-radius: 9999px;
  cursor: pointer;
  font-size: 0.8125rem;
  font-weight: 500;
  font-family: var(--edoxen-font-mono, ui-monospace, monospace);
  transition: color 150ms ease, background-color 150ms ease;
}
.edoxen-locale-tabs__tab:hover {
  color: var(--edoxen-color-primary);
}
.edoxen-locale-tabs__tab[aria-pressed='true'] {
  background: var(--edoxen-color-accent);
  color: var(--edoxen-color-background);
}
</style>
